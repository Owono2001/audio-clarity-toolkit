# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.so
*.out
dist/
build/

# Virtual environment
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# IDE / Editor specific (keep .vscode/ if you want to share launch/settings for collaborators)
# .vscode/ 
*.sublime-project
*.sublime-workspace
.idea/
*.DS_Store
nbproject/

# Configuration files with secrets (NEVER COMMIT THESE)
.env
instance/ # Flask instance folder if used for SQLite db, local secrets etc.

# Compiled files
*.pyc

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Logs and databases
logs/
*.log
*.pot
*.sqlite3 # If you ever use SQLite locally for testing
db.sqlite3
celerybeat-schedule # Celery Beat schedule file
*.pid # Process ID files

# Uploaded and processed files (these are runtime-generated)
uploads/*
!uploads/.gitkeep # Optional: to keep the empty directory in git
processed_audio/*
!processed_audio/.gitkeep # Optional: to keep the empty directory in git

# Coverage reports
coverage.xml
.coverage

# OS-specific
.DS_Store
Thumbs.db
